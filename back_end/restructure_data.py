#!/usr/bin/env

data_sources = [source_1, source_2, source_3, source_4]


def restructure_data(data_sources, start_date, stop_date, ticker_symbol):
    
    for data_source in data_sources:
        
        #open up data source
        
        for data in data_source:
            
            #if date is in range(start_date, stop_date)
                #if ticker symbol is present:
                    #restructure data in proper format (date, search_term, content, etc...)
                    #store in database
                    
                    
    